# Caddy Auth Proxy Configuration

# Required: API key for authentication
API_KEY=your-secret-api-key-here
# Required: Target domain to proxy to
TARGET_DOMAIN=example.com
# Required: Target port to proxy to
TARGET_PORT=8080
# Optional: Port for Caddy to listen on (default: 80)
PORT=80